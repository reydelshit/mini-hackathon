export type StudentTypes = {
  student_id: number;
  student_name: string;
  student_id_code: string;
  student_profile: string;
  created_at: string;
};

export type EventTypes = {
  event_id: number;
  event_title: string;
  event_type: string;
  event_date: string;
  created_at: string;
};
